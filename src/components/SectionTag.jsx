export default function SectionTag({ children }) {
  return (
    <span className="inline-block text-xs font-semibold tracking-[0.14em] uppercase mb-4"
      style={{ color: "#4f8fff" }}>
      {children}
    </span>
  );
}
